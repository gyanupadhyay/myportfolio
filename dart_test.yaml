tags:
  # Frame renders write PNGs for visual comparison against docs/reference.
  # They are platform-dependent, so CI skips them with --exclude-tags render.
  render:
    description: Renders scenes to build/frames for reference comparison.
